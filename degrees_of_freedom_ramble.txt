Degrees of freedom example:

decide the mean out of two variables> really only one datapoint.
The error is actually the sums of the errors on both variables.

correlation
abs(r)>=2/sqroot(samplresize)
